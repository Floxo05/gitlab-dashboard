## Runtime configuration for GitLab Dashboard
## Copy to .env and fill values (CapRover: set as App Variables/Secrets)

# Base URL deiner GitLab-Instanz (ohne abschließenden Slash)
# Beispiel: https://gitlab.example.com
GITLAB_BASE_URL=

# Optional: explizite Whitelist erlaubter GitLab-Hosts (kommagetrennt)
# Beispiel: gitlab.example.com,gitlab.company.local
ALLOWED_GITLAB_HOSTS=

# Starker, zufälliger String zum Verschlüsseln/Sealen des PAT-Cookies
# z. B. 32+ zufällige Bytes Base64 oder Hex
ENCRYPTION_SECRET=

# Port für Next.js in Production (CapRover setzt PORT automatisch)
PORT=3000

# Node-Umgebung (development|production)
NODE_ENV=development
